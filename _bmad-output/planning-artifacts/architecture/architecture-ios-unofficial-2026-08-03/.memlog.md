---
scope: iOS + watchOS Swift port of GlycemicGPT: iPhone app, Apple Watch app, widget/complication extensions, compile-time-linked pump Drivers, local encrypted store, optional self-hosted Backend
purpose: build-substrate
altitude: initiative
updated: 2026-08-03T15:16
---

- (constraint) VERIFIED IN SOURCE: Tandem pairing is EC-JPAKE over P-256. plugins/shipped/tandem/src/main/java/.../ble/crypto/EcJpake.kt uses BouncyCastle ECNamedCurveTable, ECParameterSpec, ECPoint and BigIntegers, with Hkdf.kt alongside and JpakeAuthenticatorTest.kt as the vector suite. It needs raw point add/multiply plus arbitrary-precision integers - not key agreement
- (constraint) VERIFIED ON THE WEB: CryptoKit exposes no EC point arithmetic. swift-crypto 4.5.1 (2026-07-16) DOES ship exactly the primitives needed - CryptoBoringWrapper/EC/EllipticCurve.swift, EllipticCurvePoint.swift (add/multiply/invert/subtract), Util/ArbitraryPrecisionInteger.swift, Util/FiniteFieldArithmeticContext.swift - but every symbol is 'package' access and Package.swift exports ONLY .library(name: Crypto). CryptoBoringWrapper, CCryptoBoringSSL and _CryptoExtras are NOT consumable by an external package
- (constraint) VERIFIED ON THE WEB: no production-grade Swift P-256 low-level EC library exists. swift-secp256k1 is the wrong curve; EllipticCurveKit states it is not for production use. So there is no off-the-shelf dependency that satisfies EC-JPAKE on P-256
- (version) GRDB.swift v7.11.1 (2026-06-18). GRDB+SQLCipher was UNBLOCKED in Feb 2026 via SPM package traits, shipped in v7.10.0 - the long-standing 'SQLCipher users are stuck on GRDB 6' constraint no longer holds. Binding GRDB 6 would have been a stale-training-data error
- (version) swift-crypto 4.5.1 (2026-07-16); swift-nio 2.101.3 available if needed
- (direction) Working mode: auto/Fast path at the user's request, mirroring the PRD run. Spine is the only deliverable - _bmad and _bmad-output are gitignored, so no artifact here ships in the repo; the contributor and dev docs the PRD requires are hand-written separately under docs/
- (decision) AD-1 paradigm: hexagonal (ports and adapters) with an actor-guarded device edge. Drivers ARE adapters, so the pattern is already the domain's shape; the PRD's read-only Capability protocols are the ports. Prevents driver code reaching into persistence, networking or UI
- (decision) AD-2 module topology: SafetyCore (zero deps) < DriverAPI < Drivers/*; DomainCore, Persistence, BackendClient beside them; AppFeature and WatchFeature above; four app-side targets (iOS app, watchOS app, widget extension, complication extension). SafetyCore is linked by EVERY target that renders or validates a glucose value - that is the mechanism for SI-4
- (decision) AD-3 dependency direction is one-way and CI-enforced: SafetyCore depends on nothing; a Driver may depend only on DriverAPI and SafetyCore, never on Persistence, BackendClient or any UI target; nothing depends on an app target. Prevents a Driver acquiring its own storage or network path
- (decision) AD-4 concurrency: Swift 6 language mode, strict concurrency complete. Each Driver owns an actor at the Core Bluetooth edge; delegate callbacks land on a dedicated queue and hop in. Domain types are Sendable value types. Prevents a background Bluetooth wake racing the UI over shared mutable state
- (decision) AD-5 Glucose is a value type storing canonical mg/dL with a THROWING init enforcing 20...500 and no clamp anywhere; mmol/L exists only as a formatting output. Never precondition - that traps the process on a background wake. Binds SI-2 and SI-3 into the type system rather than into review
- (decision) AD-6 persistence: GRDB 7.11.1 + SQLCipher, key in Keychain (device-only, after-first-unlock so overnight reconnection works while locked), forward-only numbered migrations starting at v1, no Android inheritance. Verified: GRDB+SQLCipher unblocked Feb 2026 via SPM package traits
- (decision) AD-7 Backend decoding: exactly one shared JSONDecoder configuration; every DTO writes an explicit init(from:) so unknown fields are ignored and a MISSING CONSUMED field throws; no backend-supplied value is a strict enum. Binds SI-12, whose default Swift Codable behaviour is the opposite
- (decision) AD-8 EC-JPAKE: depend on a project fork of apple/swift-crypto pinned to an exact upstream tag, adding one product that exports the existing CryptoBoringWrapper target. The maintained delta is a product declaration and an access-level change, not cryptographic code; the arithmetic stays BoringSSL's audited constant-time implementation. Rejected: hand-rolled P-256 (real failure modes on a pairing handshake), vendored C EC (same audit burden without the upstream), third-party Swift EC (none production-grade for P-256)
- (decision) AD-9 shared container: one App Group templated on the Builder Team ID. The database has EXACTLY ONE writer - the app. Widget and complication extensions read a small versioned snapshot file, never the database, so there is no second writer and no cross-process SQLCipher contention
- (decision) AD-10 Coverage Claim has one implementation, in SafetyCore. The phone computes it; the Watch renders and decays the phone's claim and never derives one. The phone-to-Watch payload is explicitly versioned so a build mismatch degrades to not-watching rather than to a wrong claim. Binds SI-6
- (decision) AD-11 background execution: Core Bluetooth state preservation and restoration is the ONLY relaunch path treated as load-bearing. BGTaskScheduler and background URLSession are best-effort and may never be the basis of a coverage claim. Prevents an implementation quietly promising cadence iOS does not guarantee
- (decision) AD-12 no therapeutic write is enforced structurally: no protocol in DriverAPI declares a write, command or set member, and CI scans Driver targets for delivery verbs and pump-write characteristic identifiers. Binds SI-1 by construction rather than by policy
- (decision) AD-14 one injectable Clock protocol supplies every now; domain and SafetyCore code may not call Date() directly, enforced by lint. Prevents freshness, decay, chart windows and day-boundary alignment disagreeing about the current time
- (decision) AD-13 error model: every fallible boundary returns a typed error; no safety-relevant path substitutes a default, a clamp or a zero on failure. A rejected reading, threshold or Safety Limit leaves last-known-good in force and is surfaced with the bound that failed
- (change) AD-6 CORRECTED. My web research said GRDB 7.10 enabled SQLCipher via SPM package traits; the reviewer checked the actual v7.11.1 manifest and there are NO traits - only commented-out lines you must uncomment ('GRDB+SQLCipher: Uncomment those lines', 'Delete the GRDBSQLite target'). Verified directly. So SQLCipher over SPM requires forking GRDB - a SECOND maintained fork beside swift-crypto
- (decision) AD-6 now: GRDB UNFORKED on plain SQLite, with at-rest protection from iOS Data Protection at CompleteUntilFirstUserAuthentication - which is what the PRD actually requires ('never NSFileProtectionComplete, which would fail background writes while the device is locked'). SQLCipher is dropped: the PRD never asked for it, I over-ported Android's choice, and it would have cost a second fork, a passphrase to manage, and a licence-allowlist entry to buy protection iOS already provides hardware-backed
- (decision) AD-16 Driver lifecycle contract (closes PRD C-1, blocking for P1): a Driver is a state machine with exactly the states DriverAPI declares; the platform owns transitions, a Driver never self-transitions; every entry point is re-entrant-safe because Core Bluetooth restoration can re-enter after termination; teardown is idempotent. Prevents five DriverAPI implementers - Tandem, Medtronic, Simulated, TraceReplay and the app-hosted Nightscout source - inventing five lifecycles
- (decision) AD-17 build configuration is a named, closed dimension: Debug, Development and Release. Composition is SUBTRACTIVE ONLY - a configuration may remove a Driver or a diagnostic surface, never add a capability, which is what keeps SI-1 true across all of them. Fault injection is a dedicated flag off in every pipeline-produced configuration; no build the pipeline produces carries a telemetry DSN. Closes the dimension AD-15's 'identically in every configuration' assumed without defining
- (decision) AD-18 the Watch app is an independently-built unit, not a renderer: it owns its own locked-readable cache and its own App Group container, schedules its own local alerts and re-alarm ladder, and joins nothing across containers by filesystem path. It still never DERIVES a Coverage Claim (AD-10 holds) - it renders and decays the phone's. The phone-to-Watch payload is one versioned envelope carrying reading, IOB, thresholds, display preferences and the claim, not the claim alone
- (decision) AD-19 history cursors are durable and monotonic-by-decode: every Driver persists its cursor before acknowledging a batch and never advances past a record that failed to decode. Binds SI-8. Process-local cursors are prohibited because iOS termination loses them, which silently loses insulin records - a cross-Driver contract no single Driver can be trusted to invent
- (decision) AD-20 every safety invariant names its enforcing mechanism in the spine, and an invariant with no mechanism is a defect rather than an aspiration. SI-5, SI-7 and SI-11 were bound by the header but had no mechanism; they now attach to the SafetyCore selector, the Driver decode contract and the validation boundary respectively
- (constraint) VERIFIED (Apple TN3115, confirmed on the web): from iOS 26, ONLY apps that use AccessorySetupKit to set up Bluetooth accessories are relaunched by Core Bluetooth state restoration after a force-quit or a Control Center Bluetooth toggle. AD-11 rested entirely on that relaunch path. The PRD's floor is iOS 17.0, so the app spans a real behaviour change and the Coverage Claim is OS-version-dependent. AccessorySetupKit appears nowhere in the PRD or the spine
- (decision) AD-21: adopt AccessorySetupKit for Pump pairing wherever the OS provides it, and make the Coverage Claim OS-version-aware. On iOS 26+ without AccessorySetupKit a force-quit or Bluetooth toggle means the app is never relaunched - a permanent not-watching state the user must clear by opening the app, which SI-6 requires be disclosed rather than assumed. AD-11 is amended: restoration is the only load-bearing relaunch path AND its availability is itself a runtime input to the claim
- (change) AD-8 CORRECTED. Verified against the source: JpakeAuthenticatorTest.kt has 7 tests and ZERO known-answer vectors - state-machine assertions, two failure paths, and a randomized round-trip against a simulated server seeded from live SecureRandom. So 'byte-for-byte agreement over its existing vectors' was not buildable as written. The conformance test now REQUIRES generating known-answer vectors from the Kotlin implementation under fixed seeds first, committing them as fixtures, then asserting Swift reproduces them. That vector-generation task is a prerequisite, not an assumption
- (change) Structural seed corrected: Medtronic is a clean-room SAKE reimplementation because Android WRAPS org.openminimed:javasake 0.2.0 (GPL-3.0) and no Swift equivalent exists; Simulated and TraceReplay Drivers are net-new with no Android counterpart, not ports
- (change) AD-4 simplified: -strict-concurrency=complete is redundant under Swift 6 language mode. Swift 6.2 default-isolation is an open choice, moved to Deferred rather than left implicit
- (change) Adversarial reviewer found a structural hole I built: my own module graph gave no two units that must agree on a data shape any module in common (AppFeature/WidgetShared and AppFeature/WatchFeature share only SafetyCore), and AD-3's CI check would fail the build on the edge that would fix it. Closed by making SafetyCore own every shared contract shape - snapshot record, phone-to-Watch envelope, Coverage Claim vocabulary, Freshness Tier
- (decision) AD-22 decode failure vs domain rejection are different outcomes: unparseable bytes = decode failure, record NOT consumed, cursor holds; parsed-but-out-of-range = domain rejection, record consumed, cursor advances, value dropped and logged. Conflating them either loses insulin records or wedges history permanently behind a value that can never become valid - and the wedge is remotely triggerable by a Backend narrowing
- (decision) AD-23 acknowledgement has exactly one owner, DomainCore on the phone. The Watch sends an idempotent acknowledgement INTENT and renders the phone's state; it never holds authoritative state. FR-74 assumed storage shared by app and Watch that NFR-4 says cannot exist
- (change) AD-5 tightened: Glucose.init validates against the ABSOLUTE bound only; narrowable Safety Limits are never a parameter to it. The idiomatic move - parameterising the initializer with current limits - would make a Backend narrowing retroactively erase stored rows on read-back, selectively toward lows, remotely
- (change) AD-9 tightened: the snapshot is ONE record written atomically (temp-then-rename) carrying schema version and production instant; an unparseable or unknown-version record renders unknown, never partial. AD-9 previously constrained who writes but not how, so a non-atomic write racing a timeline read could yield a stale value rendered Fresh
- (change) AD-10 tightened: the Not-Watching Reason VOCABULARY is versioned with the envelope. Versioning the envelope alone let a wrist knowing five reasons receive one of eight at a matching version and render it as generic 'nothing is watching' - sending the user to replace a working sensor
- (change) AD-13 tightened: the Driver failure taxonomy is declared once in DriverAPI, not per Driver, and every typed error has a NAMED recoverer. 'One typed error enum per module boundary' guaranteed two Drivers would produce incomparable taxonomies and left recovery unowned
- (event) Reviewer gate complete: lint clean, 3 lenses (rubric, currency, adversarial), 60 findings total. All criticals applied. Spine now 23 ADs
- (event) spine finalized
