
### monitoring-display — 58 reqs, 3 impossible
  R The app must present a single scrolling Home dashboard whose card order is fixed and identical to…
  R Every dashboard card must render even with no data, each showing its own empty-state message; no card…
  R The user must be able to pull down on Home to force an immediate refresh. A refresh must read pump…
  R Every time the app returns to the foreground, it must immediately re-run the pump read sequence, because…
  R Any reconciliation or refresh failure must be logged and must leave the previous cached values intact;…
  R The status row must show a pump-link indicator in every mode with six distinct states (connected,…
  R The sync and backend-reachability indicators must be hidden entirely when no backend is configured, and…
  R Backend reachability must be derived only from traffic the app already makes: any HTTP response,…
  R Device connectivity must not require validated internet access, so a LAN-only network reaching a self-…
  R When a self-hosted server is on the local network, the app must request local-network permission with a…
  R When the app is configured to reach a server over plain HTTP, a persistent warning banner must appear…
  R Each active, compiled-in driver module that ships a brand mark must display it untinted after the status…
  R The glucose hero must show the current value at hero scale with its trend glyph in the same colour, the…
  R When the glucose reading is too stale, the hero value and trend glyph must lose their severity colour…
  R Glucose severity colour must be a function of the stored mg/dL value alone with no unit input, so the…
  R All storage, transport, threshold comparison, alerting, chart geometry, Time-in-Range bucketing, CV% and…
  R mg/dL to mmol/L conversion must use exactly one constant, 18.0156, shared with the backend and the Apple…
  R The iPhone app and the Apple Watch app must produce byte-identical glucose strings for the same stored…
  R Glucose spreads (standard deviations and differences) must convert through a distinct, offset-free path…
  R Every number the dashboard displays must use a dot decimal separator and half-up rounding regardless of…
  R Changing the glucose unit anywhere — in Settings or via a backend reconcile — must re-render every…
  R Data freshness must be classified into fresh / stale / too-stale using half-open boundaries (exactly at…
  R Freshness must re-evaluate on a wall-clock timer independent of new data arriving, at one quarter of the…
  R A staleness badge must render nothing at all when fresh, 'Stale' in amber when stale, and 'Too old' in…
  R On every Apple Watch complication, Smart Stack entry and Lock Screen or Home Screen widget, the…
  R The alert-floor gate must require strictly-fresh data measured against the CGM's own sensor timestamp —…
  R Any 'this device is watching' claim must decay on its own schedule with no new data, must emit a…
  R The trend chart must render on a fixed 40–300 mg/dL Y axis with dashed grid lines and labels only at the…
  R The chart must draw, back to front: target band, threshold grid lines, Y-axis labels, X-axis time…
  R Chart time-axis label format and tick count must follow the visible span using the Android rules, and…
  R The chart legend must list only the series actually present in the loaded data.
  R From Home the user must be able to expand the chart into a detail view supporting pinch-zoom down to a…
  R The chart detail view must present in landscape and must restore the previous orientation posture on…
  R Period selection must be shared between the Home chart card and the chart detail view, and between the…
  R The dashboard must maintain five independent period selections (chart, Time in Range, CGM stats, insulin…
  R Period choices must be filtered by the user's local retention setting, and a selection outside the…
  R Time in Range must be computed by a database aggregate over readings between 20 and 500 mg/dL using the…
  R CGM statistics must report mean, sample standard deviation using the n−1 denominator, CV%, GMI, CGM-…
  R Insulin summary must report total daily dose, the basal/bolus split as a stacked bar and legend, food…
  R The app must not present a bolus's requested meal and correction portions as if they summed to the…
  R Recent Boluses must show the five most recent events in descending time order with time, units, a type…
  R BG at event and IoB at event must be cross-referenced to the nearest reading within plus or minus five…
  R Glucose thresholds must be cached locally with the defaults 55/70/180/250, refreshed from the backend…
  R Glucose readings outside 20–500 mg/dL and boluses outside 0–25 units must be rejected at the model…
  R Local pump and CGM data must be encrypted at rest using the platform's data protection so it is…
  R The tab bar must be capability-driven: Home, Alerts and Settings always present, and an AI Chat tab only…
  R The start destination must depend only on onboarding completion and never on session validity, so an…
  R A tappable session banner must appear for an expired session unconditionally, and for an unauthenticated…
  R Detail screens must hide the tab bar and provide a back affordance.
  R The app must support System, Dark and Light theme selection, applying it live without restart, using the…
  R Every element that carries a Compose test tag today must carry the same string as an accessibility…
  R Every card must expose a single combined VoiceOver description matching the Android content-description…
  R The hero value and other fixed-size numerals must remain legible and non-truncating across Dynamic Type…
  R Markdown prose rendered anywhere in the app must strip image markdown and HTML image tags before…
  R Every 'now' used by freshness classification, chart windows, day-boundary alignment, the basal integral…
  R Debug builds must include a simulated pump driver, selectable in the Simulator, that can drive every…
  R Debug builds must retain the compressed fast-staleness policy (stale at 20 seconds, too-stale at 45…
  R The Apple Watch app and the iPhone app must link the same formatting, freshness and threshold modules…
  X A persistent, non-dismissible OS-level status surface… -> A three-part honest substitute: (1) an ActivityKit Live Activity…
  X Guaranteed fixed-cadence background refresh (Android's… -> Compose three mechanisms and design the display to be honest about…
  X Shipping a custom watch face (the Android `watchface/`… -> Two things, both real and both worth shipping. (1) WidgetKit…

### alerting — 34 reqs, 7 impossible
  R The system must deliver audible glucose alerts for urgent low, low, high, urgent high, and insulin-on-…
  R The system must run with no backend configured at all and still raise on-device low and high alarms…
  R The system must never raise an on-device alarm from a CGM reading whose own sensor timestamp is more…
  R The system must never raise an on-device alarm from thresholds that neither the user nor their server…
  R The system must let a user set four alert thresholds (urgent low, low, high, urgent high) on the device…
  R The system must present thresholds as read-only with an explanation that they are edited in the web app…
  R The system must silently discard a server threshold response that fails validation, leaving the last…
  R The system must preserve device-set thresholds across sign-out, and must discard server-set thresholds…
  R The system must deliver an urgent low alert so that the user hears it while the phone is in a Focus…
  R The system must let a user choose a distinct alert sound per severity tier from a bundled set, import…
  R The system must repeat an unacknowledged urgent alert on a bounded, finite schedule until acknowledged,…
  R The system must display an alert while the app is in the foreground — a foreground app must never…
  R The system must let a user acknowledge an alert directly from the notification on iPhone or on Apple…
  R An acknowledgment made while offline must never be reversed by a later server delivery of the same…
  R The system must not re-alarm the same alert type more than once per 30 minutes while it remains…
  R The system must not re-alarm on the device for a condition the server already announced and the user has…
  R The system must tell the user at all times, on every surface it offers — in-app, Lock Screen, and Apple…
  R Every coverage claim must carry an explicit expiry time and must degrade to an unknown or not-watching…
  R The system must give a specific, actionable reason whenever it is not watching, drawn from:…
  R The system must proactively notify the user when it has stopped watching for longer than the coverage…
  R The system must warn the user, during onboarding and in settings, that force-quitting the app from the…
  R The system must resume alerting automatically after a background termination when the paired pump…
  R The Apple Watch app must show the current alert, its age computed on the watch's own clock, and whether…
  R The Apple Watch must never independently conclude that coverage is healthy — it must render the iPhone's…
  R A watch complication must reflect alert and coverage state on the watch face and must change to a not-…
  R A single fresh low must produce one wrist alert experience, not several.
  R The system must never retract a wrist alert into an 'all clear' state on the basis of a reading it…
  R The system must show the most recent 100 server alerts, prune alerts older than 7 days, offer pull-to-…
  R The system must render every glucose value in the user's chosen display unit while storing and…
  R A caregiver data-gap alert must never display a glucose value in its title, because that value is only a…
  R Tapping an alert notification must open the app to the Alerts screen.
  R The system must, in developer builds only, let the developer inject simulated CGM readings, force the…
  R The system must keep its alert database and stored credentials readable while the device is locked after…
  R The freshness policy, threshold bounds and ordering rule, unit conversion constant, re-alarm cooldown,…
  X Raising the system alarm volume for a low alert and… -> With the Critical Alerts entitlement,…
  X Bypassing Do Not Disturb, Focus, and the silent switch by… -> `interruptionLevel = .timeSensitive` with the Time Sensitive…
  X Custom per-alert vibration patterns (the low channel's [0,… -> Encode severity in the SOUND rather than the haptic — a distinctive…
  X Automatically restarting alerting after a device reboot… -> Core Bluetooth state preservation and restoration will relaunch the…
  X An ongoing, non-dismissible, indefinitely persistent status… -> A three-part composite: a Live Activity for the rich glanceable claim…
  X Holding a long-lived server connection (the SSE alert… -> APNs is the only path by which a server-originated alert reaches a…
  X A custom watch face carrying glucose and alert state, and a… -> WidgetKit accessory complications that the user places on Apple's…

### ai-and-input — 69 reqs, 9 impossible
  R The AI Chat tab must be present only when a server base URL is configured; when the app is in backend-…
  R On opening AI Chat the app must probe the server's AI provider exactly once and land in one of three…
  R A user must be able to send a chat message of up to 2000 characters inclusive; a longer message must be…
  R The chat transcript must live only in memory, capped at the most recent 100 messages, and must be lost…
  R Chat failures must always present curated copy and never a raw error message: request timeout, no…
  R The chat screen must show four tappable starter suggestions when the transcript is empty; tapping one…
  R Assistant markdown must be sanitized before rendering: all image markdown and HTML image tags are…
  R A user must be able to turn spoken responses on and off from the chat screen, and while on, choose from…
  R Speech must duck other audio rather than interrupt it, and must stop when the user turns speech off,…
  R If a chat request is interrupted because the app left the foreground, the app must say so honestly on…
  R A user wearing the watch must be able to ask the AI a question from the watch and receive an answer on…
  R When no server is configured, a watch chat attempt must return the terminal message "AI chat needs a…
  R The watch must offer system dictation, scribble, and keyboard input for a question, plus three one-tap…
  R A user must be able to place a chat complication on their watch face and a chat widget in the Smart…
  R The watch must show a specific, honest failure for each condition — phone not connected, request timed…
  R The app must not include an AI daily-brief screen. AI insights and briefs arrive only as server-pushed…
  R Meal logging must be reachable only when a server is configured and meal intelligence is enabled; when…
  R A user must be able to log a meal by taking a photo or choosing one from their library. Choosing from…
  R Photos must be downscaled to a longest edge of 1280 pixels, oriented upright, and re-encoded as JPEG…
  R Temporary capture files must be deleted after upload, on cancellation, and when a new capture supersedes…
  R The "Estimating carbs…" indicator must be impossible to leave stuck: every failure path clears it, image…
  R Every surface that displays a carb estimate must display, at the same time, "Rough estimate — an AI…
  R Carbs must always be presented as a low-to-high range with a confidence signal, never as a bare number;…
  R VoiceOver must announce the carb value, the confidence, the never-dose qualifier, and any uncertainty…
  R Confidence must be conveyed by bar length as well as color, and the medium and low levels must remain…
  R When the estimate came from multiple reads that disagreed, or the AI was unsure what the food was, the…
  R A user must be able to confirm or correct what the food is, as an action separate from correcting the…
  R A user must be able to correct the carb range. Values must be rejected — never silently clamped — when…
  R A user must be able to ask "How was this estimated?" and see how many times the photo was read, what…
  R When the server returns nutrition information the app must render it verbatim in the server's own words…
  R Heart and blood-pressure nutrition information must appear only for a confirmed, grounded record,…
  R A user must be able to save a meal's estimate as a named common food, see it confirmed by the name the…
  R When the server is unreachable, meal history and common foods must show a clear "can't reach your…
  R A user must be able to delete a meal record or a common food with immediate effect and no confirmation,…
  R A user must be able to re-log a saved common food by viewing its saved carb range in a read-only sheet…
  R Home must show the most recent meal with its carb range, confidence, timestamp, and the never-dose…
  R First-run onboarding must present welcome, features, a safety disclaimer, optional server setup, and…
  R A user must be able to finish onboarding without a server and use the app as a direct pump monitor;…
  R Both onboarding completion paths — after sign-in and after choosing local-only mode — must request…
  R A user must be able to test their server connection before signing in, see a clear success or failure…
  R If iOS blocks access to a server on the local network, the app must say exactly that and link the user…
  R A user must be able to opt in to reaching a self-hosted server over unencrypted http on a private or LAN…
  R While insecure LAN http is actually in use, a persistent banner must appear above every screen including…
  R Host classification for the LAN exception must never perform a DNS lookup, must accept only strictly-…
  R Sign-in credentials, the access token, its expiry, and the refresh token must be stored in the system…
  R The app must choose its start destination from whether onboarding was completed, independent of whether…
  R Settings must present Account, Network, Drivers, Sync, Meal Intelligence, Notifications, Watch,…
  R In backend-optional local mode, Settings must hide the backend sync control, the meal intelligence…
  R Alert thresholds must be read-only whenever a server is configured, showing the four current values, the…
  R In local-only mode a user must be able to set all four thresholds in their chosen unit. All four must…
  R On-device alarms must not fire from default thresholds. Until thresholds arrive from a server or are…
  R Alert thresholds must be written durably before the save is reported as complete, and must remain…
  R Signing out must clear server-supplied alert thresholds so the alarm floor cannot fire from a departed…
  R A user must be able to choose a sound for low alerts, high alerts, and AI notifications from a set of…
  R Low and high glucose alerts must attempt to break through Focus and Do Not Disturb, and low alerts must…
  R The notification status card must report every condition that would prevent an alert reaching the user —…
  R Settings must show whether the watch is paired, whether the watch app is installed, and whether it is…
  R A user must be able to configure what the watch app and its complications show — insulin on board, the…
  R Because watchOS does not support third-party watch faces, the app must deliver glanceable data through…
  R Settings must show the installed phone and watch app versions, flag a mismatch, and direct the user to…
  R A user must be able to choose System, Dark, or Light appearance, per device, and the choice must apply…
  R A user must be able to display glucose in mg/dL or mmol/L. All storage, transport, thresholds, alert…
  R When the app has picked a unit automatically from region or a data source, it must say so once above the…
  R A user must be able to enable or disable meal intelligence for their account, with the local effect…
  R A user must be able to set local data retention between 1 and 30 days, defaulting to 7, and that value…
  R A user must be able to open the app's open-source license documents with no connectivity, select and…
  R Debug builds only must expose a developer section with a driver debug console, a pump-label toggle, a…
  R The debug console must let a developer inject a normal glucose reading, a fresh low reading, and a stale…
  R When the session has expired, or when a server is configured but the user is not signed in, a tappable…
  X Runtime/custom plugin installation and removal — the… -> A built-in driver registry: every supported pump driver ships inside…
  X In-app self-update — checking GitHub Releases, downloading… -> TestFlight is the update channel: it notifies testers, and builds…
  X Pushing a watch app build to the paired watch — the Watch… -> Report the installed watch app version (obtained over…
  X Custom watch faces — the watch-face gallery (Digital Full /… -> Three partial substitutes, none of which is a face: (1) WidgetKit…
  X Choosing an arbitrary system ringtone or notification sound… -> Ship a curated set of sounds inside the app — a deliberately…
  X The notification channel model — per-category sound,… -> Move category granularity into the app: in-app per-category…
  X Raising the device's system alarm/ringer volume so a low-… -> Critical alerts: with the…
  X An App Transport Security exception scoped to private/LAN… -> Set `NSAllowsArbitraryLoads = YES` with no `NSAllowsLocalNetworking`…
  X A guaranteed long-running background execution window on… -> A background `URLSessionConfiguration` upload/download task, which…

### plugin-architecture — 40 reqs, 4 impossible
  R The app must ship with every supported pump and CGM driver compiled into the signed binary; the user…
  R The user must be able to view a list of available drivers showing name and version, and activate or…
  R Activating a driver that claims a single-instance capability (glucose source, insulin source, pump…
  R Multiple blood-glucose-meter sources and multiple data-sync sources must be able to be active at the…
  R Deactivating a driver must require an explicit confirmation that names the consequences (may stop…
  R The user's driver selection must survive app relaunch, device restart, and app upgrade, and must be…
  R A driver disabled by a build-time kill switch must be entirely absent from the driver list and must…
  R A driver that fails to initialize must be skipped without preventing any other driver from loading and…
  R Each driver must have private, namespaced settings storage that survives deactivation and reactivation,…
  R Pump pairing secrets (device identifier, pairing code, derived handshake secrets) must be stored in the…
  R The app must let the user pair with a pump the phone discovers by scanning, showing each discovered…
  R The app must let the user pair with a pump that connects to the phone rather than the reverse. This flow…
  R When an advertise-and-wait pairing fails, the app must show a specific reason — device does not support…
  R Connection state must be surfaced to the user across at least: disconnected, scanning, connecting,…
  R After the system terminates and relaunches the app in the background, the app must reconnect to a…
  R When a stored device identifier no longer resolves (for example after an app reinstall), the app must…
  R Drivers must expose no therapeutic write of any kind. The driver SDK must contain no bolus delivery, no…
  R Every glucose value entering the system must be rejected — never clamped, never silently corrected — if…
  R Every bolus must be rejected outside 0–25 units; every basal rate ingested from the cloud must be…
  R Rejection of an out-of-range record must be recoverable and logged with the limit that was violated; it…
  R Safety limits supplied by the backend must only ever narrow the absolute clinical bounds; a value that…
  R Safety limits must be persisted as a single atomic record and must be available to every driver as a…
  R Any read of pump history, status, or insulin data while no driver is active must return a result the…
  R The user must be able to enable a cloud data source that pulls their Nightscout-derived glucose and…
  R The cloud data source must be off by default, must retain already-synced data when disabled, and must…
  R The cloud data source must show the time of the last successful sync and a distinct state for each of:…
  R The user must be able to trigger an immediate sync on demand and see the sync state update without…
  R When the user's account has more than one active Nightscout connection, the user must be able to choose…
  R Background sync must be best-effort and the interface must never imply a freshness the platform did not…
  R A background sync run must finish within the time the system grants it or abort cleanly, persisting the…
  R Records that arrive at the same timestamp from a cloud source and a Bluetooth driver must collapse to a…
  R The app must run usefully with no backend configured: driver activation, pairing, Bluetooth data…
  R The app must be able to reach a self-hosted backend on the user's local network, must ask for local…
  R Logs must never contain glucose values, insulin doses, or backend connection credentials; raw device…
  R Drivers must be able to contribute dashboard cards and a tappable detail screen built entirely from a…
  R Plugin card ordering must be deterministic across refreshes, with platform cards above driver cards, and…
  R A setting the user types must be persisted when they leave the field or leave the screen, never silently…
  R The Apple Watch app must not host drivers and must not perform device pairing; it must display data the…
  R The app must include a built-in simulated data source that produces realistic, safety-limit-validated…
  R The app must declare its active driver's bolus-category vocabulary and its mapping to platform…
  X Runtime plugin loading and execution (DexPluginLoader:… -> Compile-time-linked Swift driver packages, which is the decision…
  X Runtime plugin package install and removal… -> Remove the flow entirely and replace the Custom Plugins card with a…
  X PluginManifest (META-INF/plugin.json: factoryClass,… -> The driver's `PluginMetadata`, declared in Swift and constructed at…
  X RestrictedPluginContext / RestrictedContext in-process… -> The iOS trust boundary moves from runtime enforcement to code review…

### data-and-background — 42 reqs, 5 impossible
  R All pump, CGM, bolus, basal, battery, reservoir, alert, raw-history and outbound-queue data must be…
  R The database encryption key must be generated once on first run, be readable only by this app on this…
  R A fresh installation must not inherit auth tokens, pump pairing secrets or database keys left behind in…
  R The local store must reject duplicates at write time: at most one CGM reading per timestamp, one basal…
  R When a cloud-sourced record and a pump-sourced record collide on the same timestamp, resolution must be…
  R Stored rows that violate current safety invariants — glucose outside 20–500 mg/dL, bolus units outside…
  R Glucose must be stored, transported, compared and threshold-tested in mg/dL everywhere; mmol/L must…
  R The user must be able to choose a data-retention window between 1 and 30 days, defaulting to 7, and the…
  R Retention must bound the alert history and the raw pump-history table as well as the six pump-data…
  R Every uploadable pump event must be durably recorded locally before any upload is attempted, and must…
  R An upload that begins while the app is in the foreground must be able to complete after the user…
  R Upload failures must be classified so that transport failures and HTTP 408, 429, 502, 503 and 504 never…
  R The outbound queue must be bounded, and when the bound is exceeded the oldest undelivered events must be…
  R The user must be able to see how many events are waiting to upload and when the last successful upload…
  R Removing the server URL must leave a fully functional local-only monitor: no undelivered upload rows are…
  R Adding a server URL must resume syncing without requiring an app restart, and signing out must preserve…
  R The app must attempt queue drain, retention, alert-threshold refresh and cloud-source sync at every…
  R The app must state, both inside the app and on a Lock Screen or Home Screen surface, whether anything is…
  R The app must never claim background coverage it cannot deliver: when monitoring has stopped because the…
  R Onboarding must tell the user explicitly that restarting the iPhone or force-quitting the app stops…
  R The initial multi-month pump history download must run as a user-visible, cancellable, resumable…
  R The user must stay signed in across app restarts; the access token must be refreshed before it expires…
  R A 401 response must trigger exactly one serialized refresh-and-retry shared across all concurrent…
  R Signing out must always win over any in-flight token refresh; a refresh that completes after sign-out…
  R A session that expires while offline must show 'session expired, sign in again' without erasing the…
  R The app must refuse plaintext HTTP to any host that is not a literal loopback, private, carrier-NAT or…
  R Reaching a server on the local network must request Local Network access, and a denial must be surfaced…
  R The user must be able to opt in to insecure LAN HTTP explicitly; that choice is per-device and must…
  R The app must report three distinct connectivity states — device offline, backend unreachable, reachable…
  R The app must be able to sound an urgent-low or urgent-high alarm while the iPhone is silenced or in a…
  R The on-device alert floor must never alarm off a reading whose own sensor timestamp is older than the…
  R A backend response carrying alert thresholds, a target glucose range, or safety limits that fails…
  R The user must be able to acknowledge an alert with no network connection; the acknowledgement must stick…
  R An ongoing low or high must continue to reach the wrist until the episode ends or is acknowledged, and…
  R The Apple Watch app must show current glucose, trend, time since the last reading, insulin on board, and…
  R The Apple Watch app must ship in the same build as the iPhone app; there must be no separate watch…
  R The app must tell the user when a newer build is available and route them to the distribution channel to…
  R The app must never issue any command that changes pump therapy; the pump interface must expose read-only…
  R The app must include a compile-time-linked simulated pump driver and an in-app fault-injection surface —…
  R Release builds must offer a user-accessible diagnostic log export, scrubbed of tokens, email addresses…
  R The app must tolerate unknown fields in every backend response and must apply the documented default for…
  R The app must register itself with the backend as an iOS device with a stable per-installation identifier…
  X PumpConnectionService — a foreground service with a… -> Three surfaces together, none of which is permanent. (1) Core…
  X BootCompletedReceiver — automatic restart after device… -> Core Bluetooth state restoration covers the case the Android receiver…
  X Persistent server-alert stream (AlertStreamService's… -> Apple Push Notification service is the only path, and it requires a…
  X In-app update checking and APK self-install… -> Keep the GitHub release check (the version comparison, the channel…
  X Wear APK update checking, download and push to the watch… -> Nothing is needed — the capability dissolves. The Watch app ships in…

### wear-surface — 42 reqs, 6 impossible
  R The system must ship one iPhone app and one Apple Watch app from a single distributable bundle, such…
  R The system must let a user place GlycemicGPT complications on their chosen Apple Watch face and see, at…
  R The system must provide guided in-app setup that tells the user exactly which complications exist, what…
  R The system must show current blood glucose on the wrist with a freshness marker: the value plain when…
  R The system must show insulin on board on the wrist with the same three-tier treatment on a pump policy:…
  R The system must make wrist freshness decay happen on the watch's own clock and without requiring any…
  R The system must never show a value on the wrist that is outside 20–500 mg/dL, at any freshness tier,…
  R The system must convert to mmol/L for display only, using the single factor 18.0156, dividing once and…
  R The system must sanitize incoming thresholds in a dependent order that guarantees 20 ≤ urgent low ≤ low…
  R The system must show a wrist coverage indicator that distinguishes three states — something is currently…
  R The system must encode the wrist coverage state so that it is unambiguous in monochrome and under an…
  R The system must render the five specific 'not watching' reasons with distinct user-facing copy —…
  R The system must have the iPhone advertise its coverage claim with an absolute expiry, and must have the…
  R The system must not let the watch derive its own coverage claim; the watch renders and decays the…
  R The system must not persist the coverage claim across a watch app restart; after a restart, coverage…
  R The system must treat a device clock that has jumped backward by more than 60 seconds as untrustworthy…
  R The system must deliver an alert to the wrist with a haptic even when the iPhone is unlocked and in…
  R The system must be able to break through Silent Mode, Do Not Disturb, and Sleep Focus for urgent low and…
  R The system must re-alarm the wrist on a sustained alert every 30 minutes, scheduled locally on the Watch…
  R The system must let the user turn wrist alerts off, must state plainly in that setting that doing so…
  R The system must never retract a shown alert on the basis of data it cannot vouch for: when a reading is…
  R The system must refresh a still-live alert on the wrist at least every 5 minutes so that an ongoing…
  R The system must let the user dismiss an alert from the wrist — from the alert screen and from the…
  R The system must acknowledge a wrist dismiss locally before attempting any server call, so the dismiss is…
  R The system must show a full-screen glucose graph on the Watch with pan-back-in-time and tap-for-value,…
  R The system must use one glucose Y-axis clamp across every graph surface, so no reading is visible on one…
  R The system must forward basal, bolus and insulin-on-board history for the last 6 hours to the Watch,…
  R The system must recover gracefully from a truncated or malformed history payload by rendering no overlay…
  R The system must let the user ask the AI a question from the Watch by voice or text, must speak the…
  R The system must tell the user on the Watch, before any loading state, that AI chat requires a configured…
  R The system must use a single AI request timeout shared by the Watch and the backend call, so a late…
  R The system must cache the most recent glucose reading, glucose history, insulin on board and display…
  R The system must sync display and overlay preferences from the iPhone to the Watch on every change and…
  R The system must show the user, in iPhone settings, the true state of the wrist link: whether a Watch is…
  R The system must budget wrist complication refreshes against the operating system's daily allowance and…
  R The system must show a glanceable glucose surface on the iPhone Lock Screen and Dynamic Island that also…
  R The system must carry full-colour glucose banding — red at or beyond urgent thresholds, amber at or…
  R The system must be developable and demonstrable end to end in the iOS and watchOS Simulators using an…
  R The system must state plainly, in the user documentation and in the app, that the Watch experience is…
  R The system must warn the user when the installed Watch app build does not match the iPhone app build,…
  R The system must share every safety-critical constant and pure decision function — the 20–500 bound,…
  R The system must expose the same accessibility descriptions on every wrist surface that the Android…
  X Custom watch faces — the DIGITAL_FULL and ANALOG_MECHANICAL… -> Three things together get most of the user value. (1) WidgetKit…
  X Watch Face Push — phone side (WatchFacePusher: asset… -> Ship the complications in the app bundle (reviewed, signed, installed…
  X Watch APK self-update over the data link… -> The watchOS app ships inside the iOS app bundle, so a TestFlight…
  X Per-buildType WFF provider-package substitution and the… -> Nothing is needed. Apple's equivalent problem does not arise: a…
  X Phone-controlled watch face theme (dark / clinical_blue /… -> Delete both from the wire contract and the settings UI rather than…
  X Triggering a wrist haptic from the background without… -> Deliver the alert as a notification scheduled on the watch (see the…

### pump-driver-api — 28 reqs, 3 impossible
  R The app must define the absolute glucose validity range 20-500 mg/dL exactly once, in a module shared by…
  R CI must fail the build if the glucose bounds, the mmol/mg-dL factor 18.0156, or the Tandem epoch offset…
  R Every reading model (CGM, BGM, bolus) must reject out-of-range construction by throwing, so a driver…
  R All history extraction and bolus-history paths must drop out-of-range readings and doses rather than…
  R Drivers must read the current safety limits at every validation pass, and a test must prove that…
  R The app must accept safety limits only from backend sync and must never offer a local override; limits…
  R The app must expose the age of the last safety-limit sync and must treat a never-synced install as stale.
  R A user must be able to see every pump driver compiled into their build, which drivers are active, and…
  R Activating a driver must never leave its capability slot empty during the swap, and activating a…
  R Deactivating a driver must stop its work while preserving its credentials and settings, so reactivation…
  R A maintainer must be able to disable a specific driver at build time such that it is absent from the…
  R A user must be able to pair a Tandem t:slim X2 by scanning, choosing from a device list and entering the…
  R A user must be able to pair a Medtronic 700-series pump by keeping a pairing screen open while the phone…
  R The app must reconnect automatically to a previously paired pump after being backgrounded, after being…
  R The app must remain able to identify a previously paired pump after the app is reinstalled or the phone…
  R When a user unpairs a pump the app must clear its stored credentials and disconnect, and must tell the…
  R The app must distinguish 'no new reading' from 'the pump has not responded', and must surface the age of…
  R The app must backfill pump history after any period of lost connectivity or app suspension, and a full…
  R Pump credentials and pairing codes must be stored encrypted, unavailable before first device unlock,…
  R Raw BLE packet payloads must be retained only in debug builds and only in memory, and must never be…
  R The app must expose no insulin delivery, basal change or pump setting write anywhere in the driver API,…
  R The app must ship a fully simulated pump driver, runnable in the iOS Simulator without Bluetooth, that…
  R The app must ship a recorded-trace replay driver so that real Tandem and Medtronic packet parsing and…
  R A user must be able to fork the repository, supply their own Apple Team ID and App Store Connect API…
  R The app must declare NSBluetoothAlwaysUsageDescription and the bluetooth-central and bluetooth-…
  R Every driver plugin ID must match the reverse-domain identifier pattern and every settings key must…
  R Event delivery between drivers and the platform must be explicitly best-effort with bounded buffering,…
  R A driver that is compiled into the app but not registered in the driver catalog must fail CI rather than…
  X Runtime plugin loading (DEX JAR sideloading via… -> Compile-time-linked driver targets — already the decided architecture…
  X Plugin JAR file management (PluginFileManager… -> Nothing user-facing survives. The equivalent trust decision moves to…
  X Runtime plugin sandbox (RestrictedContext +… -> Compile-time containment in place of runtime containment: each driver…

### tandem-driver — 28 reqs, 6 impossible
  R A user must be able to discover a Tandem t:slim X2 or Tandem Mobi from the iPhone app by scanning for…
  R A user must be able to complete first-time pairing by entering the pump's pairing code (6-digit from the…
  R The app must accept and complete an iOS Bluetooth pairing prompt as part of first connection, and must…
  R After a successful pairing the app must reconnect to the same pump automatically, without the pump being…
  R The app must keep attempting to reconnect indefinitely while a pairing exists, only stopping when the…
  R When the app determines the pairing is genuinely broken, it must say so distinctly (not as an ordinary…
  R The app must tolerate transient encryption and connection failures after at least one successful session…
  R The app must surface pump connection status to the user as distinct, actionable states: connected,…
  R While the app is in the foreground the user must see pump data refresh at Android-equivalent cadence,…
  R While the app is backgrounded the system must continue collecting pump data opportunistically whenever…
  R Every pump-derived value shown anywhere in the app or on the Watch must carry a visible data age, and…
  R The app must resume history collection from where it left off across app termination and system…
  R The app must read from the pump only. It must never issue a bolus, change a basal rate, alter a pump…
  R The app must reject any glucose value outside 20–500 mg/dL, any bolus above 25 U, and any basal rate…
  R The app must convert pump timestamps to the phone's current local time using the Tandem 2008 epoch and…
  R The app must report only completed insulin deliveries as delivered insulin, never started, cancelled or…
  R The app must categorize each bolus (Control-IQ automatic, correction, food, food-and-correction,…
  R The app must read pump battery level and reservoir volume on both t:slim X2 and Mobi, and must never…
  R The app must report the pump's model, serial number, firmware version and feature flags, and must…
  R An Apple Watch user must see current glucose with trend, insulin on board, basal rate and pump…
  R A maintainer must be able to run the complete pump protocol stack — framing, CRC, reassembly, both…
  R A developer with a Mac and a pump but no iPhone must be able to exercise the real driver against the…
  R A developer must be able to run the entire app in the iOS Simulator against a simulated pump that…
  R The pump pairing code and derived pairing secret must be stored so they are readable when the device is…
  R Diagnostic packet logs must be available to the user for troubleshooting, must never include glucose…
  R The repository's CI must gate every change to the pump driver on: static security analysis of Swift…
  R The project must document, and CI must verify, that the Swift EC-JPAKE implementation produces byte-…
  R The app must ship the required Bluetooth usage description and Bluetooth central background mode, and…
  X BluetoothGatt.refresh() — clearing the cached GATT service… -> Rely on the standards-based mechanism: implement…
  X BluetoothDevice.removeBond() — programmatic bond removal -> Detect the same conditions with the same counters, then surface a…
  X requestConnectionPriority(CONNECTION_PRIORITY_HIGH) —… -> None on the central side. The mitigating fact is that Tandem ships an…
  X requestMtu(185) and treating MTU negotiation failure as… -> Functionally a non-issue and arguably an improvement: iOS requests an…
  X A foreground service holding a permanent connection with a… -> Event-driven wakeups instead of timer-driven polling: disconnect…
  X Apple Watch connecting to the pump directly -> iPhone owns the pump connection exclusively; the Watch receives…

### medtronic-driver — 27 reqs, 6 impossible
  R A user must be able to pair a MiniMed 680G/770G/780G by opening a pairing screen in the app, seeing the…
  R The pairing screen must state, before the user starts, that the pump must first be removed from MiniMed…
  R The system must surface each distinct pairing failure with its own message and a Try Again action: no…
  R A terminal handshake rejection must latch: the app must stop advertising, must not auto-reconnect, and…
  R Once paired, the app must reconnect to the pump on its own whenever the app is running, and must tell…
  R The app must be able to receive and serve pump-initiated Bluetooth events while backgrounded, including…
  R The system must never persist secure-session cipher state across app termination; a restored connection…
  R A user must be able to unpair the pump from within the app, and the app must then instruct the user to…
  R The app must read, from the pump only: sensor glucose with a trend arrow, insulin on board, active basal…
  R The app must never write any therapeutic value to the pump. This must be enforced at three independent…
  R The app must reject rather than clamp any out-of-range physiological value: sensor glucose outside…
  R The app must reject any value decoded from a reserved IEEE-11073 no-value sentinel and must never…
  R A malformed, truncated, or hostile pump response must produce a reported read failure, never an…
  R The app must never advance a history cursor past a record the pump actually sent: an undecodable frame…
  R The app must serialize all pump exchanges so that only one is in flight at a time, and must guarantee…
  R A cancelled or timed-out exchange must leave no live notification subscription behind, so the next…
  R Glucose must be handled in mg/dL end to end; no mmol/L conversion may be introduced without a verified…
  R History timestamps must be resolved from the pump's naive local reference time interpreted in the…
  R SmartGuard auto-basal micro-boluses must remain excluded from bolus totals, and the app must state this…
  R Pump pairing identity must persist in the Keychain with an accessibility class that permits reading…
  R No log line at warning level or above may contain a health value, a raw pump payload, or a peer device…
  R The full driver below the Bluetooth transport — state machine, handshake, session reader, gateway, all…
  R A maintainer must be able to fork the repository, supply their own Apple Developer credentials as…
  R The build must ship with UIBackgroundModes bluetooth-central and bluetooth-peripheral,…
  R CI must gate on: a pinned secrets scan, a repo-authored Swift static-analysis rule set covering the…
  R The Apple Watch must display sensor glucose, IOB, active basal, reservoir, battery and pump connection…
  R The connection and pairing state must be visible to the user without opening the app, via a Live…
  X BLE advertisement manufacturer-specific data (company ID… -> Carry the identity in CBAdvertisementDataLocalNameKey ('Mobile…
  X Advertising mode, TX power, timeout and connectable-flag… -> Reduce advertisingModeFor(paired, forceFirstPair) to selecting only…
  X Discoverability by the pump while the app is backgrounded -> First-time pairing must require the app to be open and in the…
  X Programmatic bond removal on unpair… -> unpair() clears the app-side Keychain pairing record, tears down…
  X Consuming org.openminimed:javasake:0.2.0 (and… -> A clean-room Swift reimplementation of the SAKE state machine,…
  X Hosting the Medtronic driver on Apple Watch (watchOS as the… -> The iPhone remains the sole pump peer. The Watch app receives sensor…

### pairing-and-connection-ux — 32 reqs, 8 impossible
  R The app must let a user reach a dedicated pairing flow from Settings via a pump card that shows current…
  R The pairing flow presented must be determined by the active pump driver, supporting both a phone-scans-…
  R All pairing screen copy, blurbs, and code-entry instructions must be supplied by the active driver…
  R The app must expose a single connection state with the seven states disconnected, scanning, connecting,…
  R During a scan the user must see nearby pumps identified by name and detected model with a live-updating…
  R A scan must stop automatically after a bounded period of no user interaction so the radio is not left…
  R When a scan cannot start, the user must be told which specific condition prevents it — Bluetooth turned…
  R The user must be able to enter a pairing code of 6 to 16 characters with no character-class restriction,…
  R The user must be told, before the system Bluetooth prompt appears, why the app needs Bluetooth, and if…
  R While the app is connecting or authenticating the user must be able to cancel and return to the previous…
  R When pairing is rejected, the user must be shown which category of failure occurred — wrong or expired…
  R When the pump has removed its pairing information for this phone, the app must present step-by-step…
  R The app must never present the forget-device instruction, or treat a disconnection as a trust failure,…
  R Once paired, the app must attempt to reconnect to the pump indefinitely, with no attempt limit and no…
  R The app must maintain and re-establish the pump connection while backgrounded and while the device is…
  R The app must clearly and permanently inform the user that force-quitting the app from the App Switcher…
  R The user must be able to see current pump connection status from outside the app while paired, on the…
  R When the app is not watching for lows and highs, that degraded state must be communicated on a surface…
  R For a driver that requires the phone to be discoverable to the pump, the flow must tell the user that…
  R The user must be warned before pairing that a pump connects to only one phone at a time and that the…
  R Unpairing must have one behaviour regardless of where it is initiated: cancel the connection, stop any…
  R Unpairing must require an explicit confirmation before it takes effect.
  R Pairing credentials must be readable by the app while the device is locked so overnight reconnection…
  R Credentials for one pump driver must be isolated from every other driver, so selecting a different…
  R The app must never display a raw device identifier as the user-facing name of a paired pump; it must…
  R A failed reconnection or authentication that occurs while the user is not on the pairing screen must be…
  R The connection status shown on the home screen must be tappable when pairing has failed, taking the user…
  R The app must present a background-monitoring health check in Settings covering notification…
  R A maintainer must be able to exercise every pairing state, every failure mode, and the full reconnection…
  R A maintainer must be able to capture a real pairing and reconnection session on physical hardware and…
  R The watch app must expose no pairing, scanning, code entry, or unpairing capability, and must display…
  R The app must state in-product and in documentation which pump drivers are validated against physical…
  X Programmatic removal of the OS Bluetooth bond (removeBond… -> Detect the condition and hand the user an explicit, non-dismissible…
  X GATT service-cache invalidation (BluetoothGatt.refresh()) -> Handle `peripheral(_:didModifyServices:)` and re-run discovery for…
  X Medtronic advertising payload parity (manufacturer data… -> Advertise the SAKE service UUID (0xFE82 first-pair / 0xFE81…
  X Background BLE advertising visible to a non-Apple… -> Foreground-only Medtronic pairing and foreground-only Medtronic…
  X Foreground service (PumpConnectionService) as an execution… -> The `bluetooth-central` and `bluetooth-peripheral` UIBackgroundModes…
  X Wake locks and battery-threshold wake-lock policy… -> Rely on Core Bluetooth background mode, which keeps a connected link…
  X Permanently visible ongoing notification carrying the… -> A three-part substitute that preserves the safety property without…
  X Guaranteed app relaunch after user termination… -> Configure CBCentralManager and CBPeripheralManager with restore…

### ci-security-gates — 32 reqs, 7 impossible
  R The repository must define exactly five required status checks on `develop` — `Static Analysis Gate`,…
  R Every required check must either run unconditionally on every pull request or report through an always-…
  R Every required check must complete successfully on a pull request opened from a fork, using only a read-…
  R The system must run static security analysis over all shipping Swift source on every pull request,…
  R Static analysis must report, as a non-blocking warning listing the affected files, any source file the…
  R The static-analysis rule or query set must be pinned to an exact version in the repository, so that the…
  R The system must scan every resolved third-party dependency for known vulnerabilities on any change to…
  R The repository must commit its resolved dependency graph, and CI must fail if resolving dependencies…
  R Dependency-vulnerability suppressions must live in a single reviewed configuration file, must each carry…
  R The system must lint every GitHub Actions workflow and composite action for syntax, expression, context,…
  R The system must fail any pull request in which a third-party action reference is not pinned to a full…
  R The system must fail any pull request in which a composite action references the secrets context, since…
  R The system must audit workflow security posture on every pull request and fail on any finding at medium…
  R The system must fail any pull request that introduces an Xcode Run Script build phase, a Swift Package…
  R The system must fail any pull request that commits signing material or team-identifying build…
  R The system must fail any pull request whose entitlements or Info.plist introduce an App Transport…
  R The system must fail any pull request that stores credentials or pump authentication material in the…
  R Glucose must be representable only through a type whose stored value is canonical mg/dL, whose…
  R The public interface of the pump driver protocol module must be snapshotted in the repository, and CI…
  R Every pump driver's protocol framing, byte order, event-type mapping, multi-packet reassembly, and…
  R The app must be buildable and its full user-facing behavior exercisable end to end against a simulated…
  R CI must build and unit-test the phone app, the watch app, and the watch widget extension, and must lint…
  R CI must run UI tests on both iOS and watchOS Simulator destinations, publish the result bundle as an…
  R Platform-independent driver, domain, and unit-conversion code must be testable without Xcode or a…
  R The repository must ship a build-and-sign pipeline that a user runs in their own fork, under their own…
  R A maintainer must be able to verify, on demand and without exposing any credential value, that their…
  R Any credential or signing material materialized during a build must be created with restrictive…
  R The bundled third-party attribution and licence texts must be treated as application resources: a change…
  R Project lead review must be required on every change to workflows, the workflow-security allowlist, the…
  R Every developer must be able to reproduce each CI gate locally from a single committed script that CI…
  R Automated dependency updates must cover GitHub Actions pins, Swift package versions, the pinned analysis…
  R The documented list of required checks and the branch-protection configuration must be verified against…
  X Semgrep `p/kotlin` and `p/java` rule packs as the SAST… -> CodeQL with the `swift` language and the `security-extended` query…
  X Android Lint's manifest and network-security posture… -> A purpose-built `Entitlements & Plist Guard` step, as described in…
  X A project-published, project-signed application binary and… -> There is nothing to replace it with, and nothing should be. The…
  X Xcode Cloud as a repo-defined, fork-inheritable build/sign… -> GitHub Actions on macOS runners, which is what the fork-and-build…
  X Any CI verification of BLE behavior against a real device… -> Three layers, none of which is hardware in CI. (1) A `PumpTransport`…
  X A CI gate that proves a given fork's build will actually… -> A fail-fast preflight in the fork's own pipeline that checks what is…
  X CVE scanning of binary XCFramework / SPM `binaryTarget`… -> Policy rather than tooling: forbid remote `binaryTarget` dependencies…

### release-and-signing — 33 reqs, 4 impossible
  R A maintainer can promote develop to main with a single command, and that one push fans out to version…
  R The repository has exactly one version source of truth, bumped automatically from conventional commits,…
  R Every build uploaded to App Store Connect carries a build number strictly greater than any previously…
  R A user can fork the repository, add a documented fixed set of secrets to their fork, and produce a build…
  R Bundle identifiers, App Group containers, and keychain access groups are derived from the building…
  R A one-time setup workflow registers the App IDs the app needs with exactly the capabilities it uses, and…
  R The build pipeline hard-fails, never warns, if the archive is not signed by an Apple Distribution…
  R A user can validate their signing credentials in isolation — App Store Connect key authenticates,…
  R A user can re-run their build on demand and on a recurring schedule so their installed build never…
  R The app shows the version and commit it was built from, its build date, and the number of days until…
  R The app can tell the user that a newer upstream release exists than the one it was built from and deep-…
  R A user can choose whether their build comes from the stable branch or the development branch, and the…
  R Upstream CI publishes iOS Simulator and watchOS Simulator application bundles for every release and for…
  R Every direct and transitive Swift package dependency is pinned to an exact version and revision in a…
  R The Ruby release toolchain is pinned by a committed lockfile and every CI invocation runs through it.
  R A bot dependency pull request that changes package manifests has its lockfile regenerated automatically…
  R Dependency updates touching cryptography, secure storage, database encryption, the build toolchain, CI…
  R A required status check refuses to let any bot pull request auto-merge if it changes a file outside the…
  R Every dependency is checked for known vulnerabilities on every dependency change and on a schedule, and…
  R Every redistributed dependency's license is checked against an explicit allowlist and the build fails on…
  R Swift and watchOS sources are statically analyzed for security defects on every pull request by a gate…
  R Secrets are scanned across the repository on every pull request.
  R Every workflow definition is linted and security-audited, and every third-party action is pinned to a…
  R Every commit is developer-certificate-signed and free of AI attribution trailers, enforced both by a…
  R Changed-path and commit-type labels are applied automatically to every pull request and drive both the…
  R Changes to signing configuration, entitlements, release automation, dependency policy, and the safety-…
  R No build a user runs ever contains a project-owned telemetry or analytics credential; crash reporting is…
  R The Xcode version, Swift toolchain, and every CLI tool used by CI are pinned in the repository and are…
  R A maintainer can boot a paired iPhone and Apple Watch simulator, install both apps, and stream filtered…
  R Bluetooth-dependent behavior is exercised either on physical hardware by the hardware maintainer or…
  R A fork can pull upstream changes and rebuild without manual merge work, and can see which upstream…
  R The upstream project holds no signing key, certificate, provisioning profile, or Apple credential…
  R Compile-time inclusion of an individual pump driver is controlled by an in-repository build setting that…
  X In-app phone update channel client (AppUpdateChecker:… -> TestFlight is the update mechanism. Users enable TestFlight's…
  X APK download hardening (HTTPS enforcement in code,… -> Apple's own signing and delivery chain is the replacement trust…
  X Wear APK update client (WearAppUpdateChecker: phone… -> Version lockstep by construction: the watchOS app's…
  X Watch face artifact publication and its signing carve-out… -> WidgetKit complications in the accessory families (accessoryCircular,…

### contract-and-invariants — 24 reqs, 3 impossible
  R The repository must vendor a byte-for-byte copy of the backend's OpenAPI contract at…
  R A maintainer must be able to run the full contract guard suite locally on a Mac in seconds without an…
  R CI must fail any PR in which the vendored CONTRACT_VERSION does not equal info.x-contract-version inside…
  R CI must fail any PR in which an endpoint or HTTP method the app calls is absent from the pinned spec,…
  R CI must fail any PR in which the pinned spec stops declaring a field the app consumes on a safety-…
  R The project must maintain a written standing rule, enforced in review, that any newly consumed optional…
  R The app must decode responses through exactly one shared decoder configuration, and the contract tests…
  R The app must tolerate a newer backend adding unknown fields, including unknown nested objects, and must…
  R The app must fail loudly and refuse to use a response when a field it treats as required is renamed,…
  R The app must never model a backend-supplied enumerated value as a strict enum; any unrecognised member…
  R The app must read a backend contract-version signal where one is offered, compare it numerically against…
  R On first contact with a backend that is older or of unknown version, the app must continue to work on…
  R The app must show the user which upstream commit it was built from, which contract version it was…
  R There must be exactly one definition in the codebase of the 20-500 mg/dL glucose validity bound and…
  R CI must fail any PR that introduces a hardcoded glucose bound or conversion factor outside those…
  R Glucose must be stored and compared in mg/dL on every surface; mmol/L must exist only as display and…
  R Every numeric glucose string the app produces must use a dot decimal separator and half-up rounding…
  R Constructing a domain reading with an out-of-range glucose value must fail in a way the caller can…
  R The app must reject a backend settings response atomically when any threshold or safety limit is out of…
  R A user must be able to enter thresholds in either unit using their own keyboard's decimal separator, and…
  R A maintainer must be able to validate every part of this subsystem in the iOS and watchOS Simulators,…
  R Every shipped source file must carry the project's SPDX header, every file containing ported third-party…
  R CI must block any PR whose commits, code comments, or description carry AI-tool attribution, using the…
  R The upstream repository must build, test, lint, and security-scan the app without any Apple signing…
  X Guaranteed audible urgent-low/urgent-high alerting at the… -> Ship .timeSensitive as the default interruption level — its…
  X Upstream-enforced safety gates on the binary each user… -> Make the gates unbypassable-by-default inside the build workflow the…
  X A Swift SAST ruleset with the breadth of Semgrep's p/kotlin… -> Compose equivalent coverage in substance from three tools rather than…

### docs-and-policy — 46 reqs, 7 impossible
  R The published docs tree must use one _meta.json per section directory with the schema {"title": string,…
  R CI must fail a documentation PR when a page under a section directory lacks title: or description:…
  R CI must fail a documentation PR containing an internal link that resolves to nothing, evaluated against…
  R iOS documentation pages must not share page basenames with the Android repository's pages; platform-…
  R A push to main touching docs/** must dispatch a docs-updated repository event to the website repo using…
  R A user must be able to go from a fresh fork to a working app on their own iPhone using only the…
  R The build-and-install page must state the 90-day TestFlight build expiry and the exact action required…
  R The app must surface its own build expiry date and source tag in Settings so a user can see staleness…
  R The project must never hold, request, or transmit any user's Apple signing material; all signing…
  R The build pipeline must produce a signed, TestFlight-uploadable build from a fork in which no project-…
  R The app must ship an in-app license viewer reachable from Settings, with distinct Loading, Loaded and…
  R The license viewer must display, in order: the project license notice, the third-party source-lineage…
  R License texts and the GPL must be rendered as plain text and never as markdown, and repository-relative…
  R The shipped license assets must be generated from the resolved dependency graph on every build, covering…
  R The build must fail — not warn — when the resolved component list is empty, when a component has no SPDX…
  R The build must fail when a dependency's license is not on the checked-in allowlist; EPL-1.0 must not be…
  R Every per-package license exception must be pinned to an exact version and carry a written reason, so a…
  R An automated test must assert byte-for-byte equality between each shipped license asset and its on-disk…
  R Editing README.md, LICENSE, docs/THIRD_PARTY_LICENSES.md or docs/licenses/** must invalidate cached test…
  R Every in-scope Swift, shell and workflow file must carry the SPDX identifier GPL-3.0-only and the…
  R A published acknowledgments page must state, per upstream project, whether its code was studied and…
  R A repo-internal, unpublished provenance record must state, per Swift module, which Android source file…
  R The published medical disclaimer must state that no therapeutic write surface exists anywhere, that no…
  R The published alert-delivery warning must enumerate every iOS-specific condition that alert delivery…
  R No product surface may claim that alerts are guaranteed, undismissable, or capable of overriding the…
  R The published README must carry an iOS-specific per-device verification table including a 'validated on…
  R Onboarding must present a safety-acknowledgement page that cannot be skipped or swiped past, requires an…
  R The published privacy document must enumerate every outbound connection by name, state that each is…
  R Any error-monitoring integration must compile with an empty DSN in every pipeline-produced build, must…
  R Credentials must be stored in the Keychain with a device-only, after-first-unlock accessibility class;…
  R Security reports must have two private channels and no public issue path, and the policy must classify a…
  R The security policy must pre-decline, in writing, any report proposing that the absent therapeutic-write…
  R Every commit must carry a valid Signed-off-by trailer, enforced server-side with no exemption tier and a…
  R Pull requests must be blocked when AI attribution appears in commit trailers, in added source lines, or…
  R Required pull-request checks must cover, in substance: build and unit tests on iOS and watchOS Simulator…
  R The Swift static-analysis gate must fail closed if the scanner errors, and must fail if it reports zero…
  R Fork-originated pull requests must run every required check with no maintainer action, and no privileged…
  R A driver contribution must declare which capability protocols it implements from the read-only set, must…
  R The three cross-repo safety constants — 18.0156, the 20 to 500 mg/dL validity bound, and the…
  R Glucose must be stored and compared in mg/dL at every persisted field, bound, threshold, alert…
  R The app must vendor a monotonic CONTRACT_VERSION integer and a byte-for-byte-pinned OpenAPI document,…
  R Contract tests must assert the presence of every nullable consumed field whose absence would silently…
  R The repository must ship a code of conduct with a reporting address distinct from the security address,…
  R Documentation-only pull requests must receive a docs label and a Documentation changelog section without…
  R The watch setup page must state that the watch app installs automatically with the phone app, that a…
  R An in-app diagnostics surface must report Bluetooth manager state and authorization, Background App…
  X A project-published, project-signed installable binary… -> The fork-and-build model already chosen, following Loop and…
  X Custom watch face (the watchface module, Watch Face Format,… -> WidgetKit complications in the accessoryCircular, accessoryCorner,…
  X ADB-based install, sideloading, and the separate watch-app… -> The watch app is embedded in the phone app's bundle and installs…
  X In-app 'Check for updates' that downloads and installs a… -> TestFlight delivers updates automatically once the user's fork…
  X Guaranteed or undismissable alert delivery, and the… -> A layered, honestly-described alerting posture: .timeSensitive…
  X Runtime-loaded plugin contribution path (plugins/example/) -> Every driver is a compile-time-linked SwiftPM target under…
  X Battery-optimization exemption as a documented, user-… -> Document what the user CAN control and be explicit about what they…