---
topic: GlycemicGPT iOS PRD
updated: 2026-08-03T12:36
---

- (decision) Skip product brief, go straight to PRD — The maintainer is sole decision-maker, no stakeholder alignment needed upstream; Discovery covers the same ground
- (decision) Product is a Swift port of lumose-health/android-unofficial to iPhone + Apple Watch; distribution via TestFlight in the manner of xDrip4iOS/Loop; must carry the same security-testing regime and develop->main branching strategy as the Android repo
- (decision) Working mode: Fast path — batch remaining gaps, draft full PRD with [ASSUMPTION] tags, iterate on review
- (decision) Pump drivers become compile-time-linked Swift modules on iOS, not runtime-loaded plugins — iOS forbids dynamic code loading; community driver contributions become PRs + release rather than drop-in plugins
- (decision) Scope = full parity with android-unofficial as it exists today: every supported pump, full pump functionality, every feature. Not a thinner first cut
- (decision) Dev/validation loop: Mac purchased explicitly for this; iOS Simulator used to validate both the iPhone app and the Apple Watch app during development
- (decision) Watch is in scope for v1 alongside the phone app, not deferred
- (decision) Distribution follows the Loop/xDrip4iOS pattern: users build from source (Xcode or fork-and-build GitHub Actions) and sign with their OWN Apple Developer account into their OWN TestFlight. Project ships source, not binaries — this resolves the GPL-3.0-only vs Apple App Store terms conflict and replaces Android's GitHub Releases APK model
- (assumption) Hardware: The maintainer has a Tandem pump and a paid Apple Developer account but NO iPhone and NO Apple Watch (Simulator only). Maintainer DanielDanielson has an iPhone and will test. Open gap: no single person confirmed to hold iPhone + Tandem pump together for end-to-end BLE validation
- (decision) Repo must ship iOS/watchOS-specific /docs plus contributor guides. docs/** on main fires a repository_dispatch to lumose-health/website; per-directory _meta.json 'pages' array is the publish allowlist. Repo is public, so 'not public facing' can only mean 'not published to glycemicgpt.org' — genuinely internal material cannot live in this repo at all
- (decision) Nightscout is cloud-mediated, NOT a direct Nightscout client — verified in NightscoutSyncEngine.kt: pulls from backend endpoint GET /api/integrations/nightscout/{id}/data?since=... into the same local tables BLE drivers write, returns NoBackend in BLE-only mode. iOS port carries no Nightscout networking or API-secret handling
- (assumption) Maintainer DanielDanielson has iPhone + Apple Watch + Tandem t:slim X2 — closes the end-to-end BLE validation gap; Tandem can target Verified status rather than protocol-implemented-unverified
- (decision) Urgent-low alerts ship on UNNotificationInterruptionLevel.timeSensitive with runtime auto-upgrade to .critical if the entitlement is ever present. Critical Alerts is structurally unobtainable under fork-and-build (granted per Team ID); blocking on it would block forever, and per-user requests would split the userbase into two silent alarm behaviours. Recorded as a forced loss in the Parity Ledger and stated on the docs surface
- (decision) Medtronic ships present-but-gated at BETA/unverified, matching Android's existing label. Transport spike split in two: (a) can Core Bluetooth return a CBPeripheral for a central that connected to our CBPeripheralManager - answerable without a pump, done FIRST; (b) does the 780G discovery filter accept an iOS advertisement - needs hardware, left open for a community validator
- (decision) The fork build-and-sign pipeline is a SUPPORTED product surface: CODEOWNERS-gated, SHA-pinned, inside SECURITY.md response scope, reusing the existing Workflow Lint + Workflow Security gates. SECURITY.md states explicitly that key custody, Apple account state, certificate lifecycle and the 90-day TestFlight rebuild remain the user's
- (decision) APNs deferred for v1: ship SSE-while-alive + on-device alert floor + opportunistic pulls, with the device-token path reserved but unwired. Per-deployment APNs is the right eventual design but is work in the platform repo, which this PRD cannot discharge. Degraded iOS caregiver alerting recorded in the coverage contract and Parity Ledger; revisit as a platform-repo fast-follow
- (decision) Maintainer DanielDanielson validates by building from his OWN fork under his OWN Apple Developer account, not as a tester on a project-held TestFlight - adding him as a tester would make the project convey a binary and reopen the GPL-3.0 vs Apple DPLA conflict the fork-and-build model exists to avoid
- (event) Research complete: 14 subsystems mapped and port-analysed across 2 workflow runs (32 agents, ~5.06M subagent tokens). 543 capabilities, 205 safety-critical items, 535 candidate iOS requirements, 78 platform-impossible, 206 needing redesign. Persisted to research/
- (event) Spine authored directly (S0 Purpose, S1 Vision, S2 Target User + UJ-1..UJ-6, S3 Glossary, S4 Safety Invariant Register SI-1..SI-12). Section drafting fanned out: 12 feature sections with allocated FR ranges FR-1..FR-236, 4 back-matter sections, 1 consistency critic
- (decision) C11: CalibrationTarget REMOVED from the iOS Capability set (closed set becomes six). No shipped Driver implements it, it is the only member requiring a device write, and keeping it forced SI-1 to carry a carve-out weakening the product's strongest safety claim. Recorded as a deliberate divergence; a future CGM Driver needing calibration is a PRD change with its own safety review
- (decision) C6: alert sounds are a bundled curated set only in v1. VERIFIED in AlertNotificationManager.kt:131-175 that Android uses the system RingtoneManager picker over the whole system library and ships no res/raw - so iOS has no equivalent API and a bundled set is the forced substitute, not a preference. User-imported audio deferred (adds a transcode failure mode to an alarm path). Silent option retained as real parity
- (decision) C17: one chart Y-axis rule on every graph surface - default 40-300 mg/dL, EXPANDING to include any out-of-range reading, never pinning to a boundary. Pinning draws a valid in-bounds reading at a false position, which is the Android display defect; expanding preserves Android's readable default without the misleading pin
- (decision) C4: the cleartext requirement is the in-app policy (no HTTP to any host that is not loopback/RFC1918/CGNAT/link-local, classified by strict literal parsing, never by DNS). The Info.plist ATS configuration achieving it is architecture-to-verify - whether NSAllowsLocalNetworking covers raw private-IP literals is genuinely unsettled and must not be asserted. Raised as an Open Question
- (event) First full draft complete: 121,655 words across 16 section files + spine. Consistency critic: FR-1..FR-236 each defined exactly once and in range, no orphaned safety invariants, but 20 contradictions / 22 duplications / 14 vocabulary violations / 12 gaps. PM ruled on all 20 contradictions (C1-C20); reconciliation pass running
- (decision) BLOCKER 1 fixed: the accessibility parity target is a GENERATED REGISTRY diffed by CI, not a pinned count. I recounted against the Android source - grep -rn testTag over app/src/main + wear-device/src/main returns 249 (252 repo-wide), not 253; contentDescription returns exactly 113. The 253 figure was a grep-token artifact and pinning it created an unreachable acceptance criterion that would drift with every Android change
- (decision) BLOCKER 2 fixed: Backend-supplied Safety Limits are REJECTED ATOMICALLY, never clamped. FR-32 had a clamp-on-write/clamp-on-read path contradicting FR-155 and SI-11 on an SI-11 surface. A clamp silently transforms a malformed or hostile payload into a plausible one - exactly what SI-2 exists to prevent. FR-32 now conforms to FR-155
- (decision) BLOCKER 3 fixed: FR-237 extended to require the iOS security-testing page. PL-57, PL-64 and PL-66 named a published security-testing page as their disclosure surface but no FR required it to exist - the same defect class FR-237 was created to close for the troubleshooting pages. Citations re-pointed at FR-237
- (event) PRD assembled: 144,819 words, 237 FRs (FR-1..FR-237), 17 top-level sections. FR integrity verified mechanically - each FR defined exactly once, in range, nothing renumbered. All ID spaces (SI/UJ/NFR/PL/PD/PA/NG/OQ/A) resolve. Section source files retained under sections/
- (change) Docs model CORRECTED against the verified mechanism in lumose-health/website. Read docs-sources.yaml and scripts/sync-docs.ts directly: publication is a build-time PULL, each source repo extracted into content/docs/<targetPath>/ with its own namespace. My earlier merged-namespace/basename-collision requirement was factually wrong and is withdrawn - FR-220 repurposed in place into the docs publication contract
- (decision) Docs requirement has THREE parts, and part (c) is outside this repo: (a) the docs/ tree here, (b) the docs-updated dispatch here, (c) a docs-sources.yaml entry in lumose-health/website. Without (c) nothing publishes and it fails silently. Evidence: android-unofficial ships notify-docs-update.yml but has NO registry entry, so its docs are not on the website today despite a green dispatch and a green deploy. Recorded as PD-47, risk R-23, and OQ-57
- (decision) externalLinks is the only mechanism by which a doc's ../FILE.md reference resolves - sync rewrites to a GitHub blob URL, but only for listed filenames, and it keys on FILENAME not resolved path. FR-220 now requires the canonical seven (LICENSE, MEDICAL-DISCLAIMER, CONTRIBUTING, SECURITY, PRIVACY, CODE_OF_CONDUCT, README), a checked-in declaration, a two-direction in-repo CI check, and a rule that no file inside docs/ may share a basename with an entry
- (decision) Docs are greenfield and website-audience: docs/ here is empty today, every page authored fresh, nothing gathered/migrated/mirrored from the platform, org or Android repos, cross-references are links to the published site. Reason: a copy drifts undetectably and the sync rm -rf's the target path on every build, so there is nowhere for a copy to live. Link checking validates the published path shape, not the GitHub-relative one
- (event) prd.md re-assembled after the docs correction: 148,961 words, 237 FRs, 17 top-level sections. OQ-63 open: which targetPath this repo claims (existing namespaces are platform/ and about/; the value becomes the public URL prefix)
- (change) SCOPE CORRECTION from product owner: the PRD was carrying website-repo plumbing as requirements. Every repo in the org keeps a /docs folder of public pages and the website repo pulls them in; that ingestion is not this PRD's concern. Removed the docs-sources.yaml/targetPath/externalLinks requirement (FR-220 part c), PD-47, risk R-23, OQ-57 and OQ-63
- (decision) FR-220 repurposed a second time - now 'The published documentation set for the iPhone and Apple Watch apps'. Specifies the sections and pages a user needs (index, install, daily-use, apple-watch, troubleshooting, reference, concepts, dev) mirroring the org convention in GlycemicGPT, android-unofficial and .github. Content requirement, not plumbing
- (decision) Link rule simplified to one in-repo invariant: relative links are used and checked within this docs/ tree; every link that LEAVES it is absolute (published glycemicgpt.org URL for platform/org docs, GitHub blob URL for repo-root files). Removes all dependency on website-side link rewriting and holds however the site routes the tree
- (event) prd.md re-assembled: 144,703 words, 237 FRs (FR-1..FR-237, each defined exactly once, no duplicates), 17 top-level sections. Note: an over-broad delete during this pass removed section 16 from 11-16-closing.md; recovered intact from the previously assembled prd.md
- (change) CORRECTION to an earlier entry in this log: maintainer DanielDanielson was referred to with he/his in one entry. Their pronouns have not been stated. The PRD itself uses they/them consistently (verified across all 26 references); this log now matches
- (event) Reviewer gate complete: 4 parallel reviewers, ~1.16M subagent tokens. Rubric: 6 of 7 dimensions strong. Safety: 39 findings, 3 critical. Downstream: 168 findings across architect/UX/planner lenses. Parity: 97 findings, 3 critical. Full reviews in review-{rubric,safety,downstream,parity}.md
- (decision) SAFETY CRITICAL F-1 fixed: the Alert Floor stood down and the Coverage Claim reported Backend Active whenever the SSE stream was merely connected. But FR-140 states Glucose Readings are never enqueued, so a Backend configured for AI Chat (or with Nightscout off, its default) holds no glucose - the app would stand down and claim coverage while nothing watched. 'Backend alerting degraded' now requires positive evidence of a Backend-side Glucose Reading newer than the Fresh boundary, and is pessimistic when that evidence is unavailable
- (decision) SAFETY CRITICAL F-2 fixed: FR-155 validated Safety Limits ordering and bounds but no minimum width, so min=100/max=101 passed and FR-32's live gate would discard every reading while the app reported NO_FRESH_READING - an unbounded monitoring kill switch reachable from Backend misconfiguration, in the direction SI-11 permits. Now requires >=100 mg/dL width and must contain all four Alert Thresholds
- (decision) SAFETY CRITICAL F-3 fixed: FR-72's monotonic clock high-water mark had no persistence rule, bound or reset, so one forward jump (timezone, NTP step) made every later real time read as a rewind and suppressed the Alert Floor indefinitely. Now process-local, never persisted, resets on forward correction, hard-bounded at 5 minutes
- (decision) Not-Watching Reason set OPENED from five to eight (added BACKEND_HAS_NO_DATA, SAFETY_LIMITS_TOO_NARROW, CLOCK_UNTRUSTED) with a rule that a new failure mode gets a new reason. All four silent-failure paths were silent only because they collapsed into a reason that was true-sounding and wrong - NO_FRESH_READING sends the user to replace a working sensor
- (decision) PARITY CRITICAL C1 fixed: Android keeps TWO threshold sets - GlucoseRangeStore (display/target, read by GlucoseHero) and AlertThresholdStore (alarming, read by AlertFloor). Verified by consumer analysis. The PRD collapsed them into one, so hero banding, chart grid lines, wrist banding and TIME IN RANGE would compute off alarm thresholds - iOS reporting a different TIR percentage than Android. Target Range added to the Glossary as a distinct term and re-pointed across 5.3 and 5.7
- (decision) OQ-49 RESOLVED: upstream repository stays public indefinitely. Removes the ~10x macOS runner multiplier and the standing cost pressure on the five Required Checks (R-14)
- (decision) OQ-50 RESOLVED: one App ID set per Builder, registered once; the channel is a build-time branch choice (develop=development build, main=production build) and a new build REPLACES the installed one. VERIFIED against LoopDocs rather than assumed - Loop's browser build registers one identifier set per app, and a second concurrent app needs its own distinct set (as LoopCaregiver does)
- (decision) OQ-48 RESOLVED: fault injection gated on a dedicated compile-time flag OFF in every pipeline-produced build in BOTH channels; only a local Xcode build enables it. Gating on 'not TestFlight' was rejected because the develop channel ships through TestFlight too, so that test would disable the Developer section for legitimate development builds
- (decision) OQ-56 RESOLVED by the product owner: no legal counsel will be obtained. Published position is that a Builder who forks, supplies their own Apple credentials and builds their own binary already stands in the builder relationship; the project supplies only source. The disclaimer states that relationship FACTUALLY and asserts no legal conclusion, mirroring established OSS iOS diabetes projects. Recorded as an explicit owner decision made with the risk stated; this PRD offers no legal advice
- (decision) OQ-36 RESOLVED: SI-12 carve-out approved; wideSpread and identityAgreement marked optional-with-default in the Contract Pin so the exception is explicit in the contract rather than implicit in the client
- (decision) OQ-45 ASSIGNED TO ARCHITECTURE (product requirement settled, mechanism open): which Info.plist ATS configuration achieves the FR-150/NFR-20 cleartext policy, verified empirically on a device. No section may state a plist posture as fact before that baseline is recorded
- (decision) C-2 DEFECT CLOSED: FR-23 and FR-30 described the capability-slot mechanism differently (persisted slot table vs derived from active set). Reconciled to one model - the SET OF ACTIVE DRIVER IDS persists, slots are DERIVED from it by a total deterministic function with a stable id tiebreak, no slot table exists, so the divergence an earlier draft resolved by precedence cannot arise
- (change) Build model corrected against verified LoopDocs sources rather than inference: six secrets (TEAMID, FASTLANE_ISSUER_ID, FASTLANE_KEY_ID, FASTLANE_KEY, MATCH_PASSWORD, GH_PAT) with GH_PAT needing workflow scope; fastlane match against a private Match-Secrets repo in the Builder's own account; org-level secrets recommended for multi-app Builders; scheduled rebuild is a weekly change-triggered build PLUS a monthly unconditional build, because a quiet upstream can otherwise let a build lapse at 90 days
- (decision) Editorial polish: ran a mechanical proofread (unbalanced emphasis, duplicated words, malformed tables) - all 24 hits were false positives from glob patterns like **/Podfile.lock and escaped pipes in code spans. Deliberately did NOT run the cut-oriented structural editorial pass: the rubric reviewer judged shape fit strong and length earned, and a structural editor instructed to propose cuts would remove content four reviewers had just validated. Reversible on request
- (event) PRD FINALIZED. status: final, updated 2026-08-03. 148,555 words, 237 FRs, 17 sections, 8 user journeys, 12 safety invariants, 136 Parity Ledger rows. Artifacts: prd.md, addendum.md, .memlog.md (53 entries), research/ (14 subsystem analyses + 3 critic passes), sections/ (16 source files), review-{rubric,safety,downstream,parity}.md
